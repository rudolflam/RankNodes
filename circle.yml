machine:
  python:
    version: 2.7.12
dependencies:
  pre:
    - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y; sudo apt-get update; chmod +x ./install_graphtools.sh;sudo ./install_graphtools.sh; sudo apt-get update; sudo apt-get install python-graph-tool -y
test:
  override:
    - cd ./ranknodes/test/ && python test.py -v

